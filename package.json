{
  "scripts": {
    "postinstall": "cd client && yarn install",

    "postinstall": "cd client && yarn install",

    "postinstall": "cd client && yarn install"
  },

  "name": "Softwitch",
  "private": true,
  "dependencies": {}
}
